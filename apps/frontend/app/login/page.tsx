import { LoginPage } from "@/components/auth/login-page"

export const metadata = {
  title: "Login | Shifty",
  description: "Sign in to your Shifty account",
}

export default function Login() {
  return <LoginPage />
}
